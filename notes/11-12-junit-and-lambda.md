Created: 202506181430

Tags: 

```
LAMBDA EXPRESSION
	- chỉ xảy ra khi chơi Inheritance/Interface
	- chỉ xảy ra khi chơi với Interface mà chỉ có duy nhất 1 hàm abstract
		Interface chỉ có duy nhất 1 hàm abstact/hàm ko có code dgl FUNCTIONAL INTERFACE
	- lambda là hiện tượng gọi hàm mà không cần gọi đến cái tên
	- FunctionalInterface : là loại interface chỉ có duy nhất 1 hàm abstact 
		- vì chỉ có 1 hàm, sau này khi new() vùng tam chỉ có 1 hàm, dẹp mẹ tên hàm khi xài vì không có sự nhầm lẫn giữa hàm này và hàm kia
	- lambda expression CHỈ ĐƯỢC SỬ DỤNG KHI CHƠI VỚI INTERFACE MÀ CHỈ CÓ 1 HÀM DUY NHẤT


vd pv java: trong inteface hàm có quyền có code hay không
		=> có, nếu là static/default
		=> trong cheatsheet oop có giải thích
```

-----
## References
1.
